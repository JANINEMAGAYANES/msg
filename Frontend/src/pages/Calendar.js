import React from 'react';
import TimeSlots from '../components/Timeslots';

const Calendar = () => {
    return (
        <div>
            <TimeSlots/>
        </div>
    );
};

export default Calendar;
