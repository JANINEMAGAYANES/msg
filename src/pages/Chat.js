import React from 'react';
import ChatHistory from '../components/ChatHistory';

const Chat = () => {
    return (
        <div>
            <ChatHistory/>
        </div>
    );
};

export default Chat;
